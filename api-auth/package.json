{
  "name": "api-auth",
  "version": "1.0.0",
  "description": "API de autenticación con Google OAuth 2.0 para menús digitales",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@libsql/client": "^0.5.0",
    "hono": "^3.12.0",
    "jose": "^5.2.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20231218.0",
    "typescript": "^5.3.3",
    "wrangler": "^3.22.0"
  }
}
